doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title #{docTitle}
        link(rel="stylesheet", href="/index.css")
    body 
    header
        nav
            ul
                li
                    a.link(href="/", class=(path=== "/" ? "linkActive" : "link")) Shop
                li
                    a.link(href="/admin/add-product", class=(path=== "/admin/add-product"? "linkActive" : "link")) Add product
    block content
    main
        